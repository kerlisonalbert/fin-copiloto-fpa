# Segredos — NUNCA subir
.env
*.key
*.pem
credenciais*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Dados reais (só sintéticos entram no repo)
dados/reais/

.pytest_cache/

# capturas geradas em runtime (ficam no workspace do agente)
relatorios/
